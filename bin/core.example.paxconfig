core_resourceDirectory = ${CURRENT_DIR}/../res

core_windowTitle = PaxEngine3

core_resolutionWidth = 800
core_resolutionHeight = 600

core_startInFullscreen = false

# constraint: core_targetFPS <= core_targetUPS
core_targetFPS = 60
core_targetUPS = 100

# TODO: Move this to a config file for PlatformerDemo because it is game specific
physics2d_pixels_per_meter = 16