OpenGL_Hardcodes entfernen:
    OpenGLMesh.render -> glEnableVertexAttribArray momentan mit 0, bitte variabel
    OpenGLTexturedMesh ist nur mit einem Shader moeglich. Shader besser abstrahieren!
    Mehrere Texturen unterstetzen glActiveTexture(GL_TEXTURE0) in OpenGLTexturedMesh ist keine Loesung

EntitySystem
    - Arbeitet global und erfasst aktuellen Status der aktiven Welt nicht! Sollte nur fuer die aktuelle Welt arbeoten
    
Entity
	- Wenn eine Component entfernt wird sollen alle Components automatisch entfernt werden, deren Dependencies nicht mehr erfuellt sind.